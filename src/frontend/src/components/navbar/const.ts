/**
 * Arbitrary constants used in navbar
 */

// array indices of [label, link] pairs passed as props to navbar
const LABEL_INDEX = 0;
const LINK_INDEX = 1;

export {
    LABEL_INDEX,
    LINK_INDEX
}